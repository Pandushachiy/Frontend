---
description: Design System Guide — цвета, типографика, отступы, скругления
globs:
  - app/src/**/ui/**/*.kt
  - app/src/**/presentation/**/*.kt
  - app/src/**/screens/**/*.kt
  - app/src/**/components/**/*.kt
alwaysApply: false
---

# Design System Guide - Полная система дизайна

## 🎨 Color Palette

### Основные правила
- **НИКОГДА** не используй `Color(0xFF...)` напрямую в UI компонентах
- **ВСЕГДА** используй `MaterialTheme.colorScheme.*`

### Правила использования цветов

| Элемент UI | Цвет фона | Цвет текста |
|------------|-----------|-------------|
| **Экран** | `background` | `onBackground` |
| **Карточка** | `surface` | `onSurface` |
| **Кнопка основная** | `primary` | `onPrimary` |
| **Иконка активная** | - | `primary` |
| **Иконка неактивная** | - | `onSurfaceVariant` |
| **Текст заголовок** | - | `onSurface` |
| **Текст описание** | - | `onSurfaceVariant` |

---

## 📝 Typography System

### Основные правила
- **НИКОГДА** не используй `fontSize`, `fontWeight` напрямую
- **ВСЕГДА** используй `MaterialTheme.typography.*`

| Стиль | Применение |
|-------|------------|
| `headlineLarge` | H1 заголовки экранов |
| `headlineSmall` | H3 подсекции |
| `titleLarge` | Заголовки карточек |
| `titleMedium` | Имена, названия |
| `bodyLarge` | Основной текст |
| `bodyMedium` | Второстепенный текст |
| `bodySmall` | Hints, timestamps |
| `labelLarge` | Текст кнопок |

---

## 📏 Spacing System

### Основные правила
- **НИКОГДА** не используй произвольные `.dp` значения
- **ВСЕГДА** используй `Spacing.*` константы

```kotlin
object Spacing {
    val extraSmall = 4.dp
    val small = 8.dp
    val medium = 16.dp
    val large = 24.dp
    val extraLarge = 32.dp
    val screenPadding = 16.dp
    val cardPadding = 16.dp
    val itemSpacing = 12.dp
}
```

---

## 🔲 Corner Radius

```kotlin
object CornerRadius {
    val small = 8.dp
    val medium = 12.dp
    val large = 16.dp
    val extraLarge = 24.dp
    val full = 50.dp
}
```

| Элемент | Скругление |
|---------|------------|
| Кнопки | `medium` (12dp) |
| Карточки | `medium` (12dp) |
| Модальные окна | `large` (16dp) |
| Chips | `full` (50%) |

---

## ✅ Чеклист

- [ ] Нет `Color(0xFF...)` в UI коде
- [ ] Все цвета через `MaterialTheme.colorScheme.*`
- [ ] Нет `fontSize`, `fontWeight` напрямую
- [ ] Все отступы через `Spacing.*`
- [ ] Используются `CornerRadius.*`
