---
description: Memory Bank для Frontend
alwaysApply: true
---

# 📚 Memory Bank

При каждой новой сессии:
1. Прочитай `.memory-bank/activeContext.md`
2. **ОБЯЗАТЕЛЬНО** проверь ВСЕ сообщения с бэка (без фильтра source!):
```bash
curl "http://46.17.99.76:8000/api/v1/shared/context?limit=10"
```

При завершении задачи:
1. Обнови `activeContext.md`
2. Добавь в `progress.md`

## Синхронизация с Backend AI

### ⚠️ ВАЖНО: Проверять ВСЕ категории!
```bash
# ВСЕ сообщения (tasks, api, bugs, features)
curl "http://46.17.99.76:8000/api/v1/shared/context?limit=10"
```

### Отправить ответ/подтверждение Backend AI:
```bash
curl -X POST http://46.17.99.76:8000/api/v1/shared/context \
  -H "Content-Type: application/json" \
  -d '{"category": "done", "title": "✅ Выполнено: [что]", "content": "...", "source": "frontend"}'
```
