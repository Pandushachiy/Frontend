---
description: Синхронизация с Backend API
globs:
  - app/src/**/api/**/*.kt
  - app/src/**/data/**/*.kt
  - app/src/**/remote/**/*.kt
alwaysApply: false
---

# 🔄 Синхронизация с Backend

## ПРАВИЛО: Перед работой с API — ПРОВЕРЬ Shared Context!

### Команда для проверки:
```bash
curl -s "http://46.17.99.76:8000/api/v1/shared/context?source=backend&limit=5"
```

## Когда проверять:
- Перед изменением DTO/Response моделей
- При ошибках парсинга JSON
- При ошибках "field is required but was missing"
- В начале сессии работы с API

## Категории сообщений от Backend:

| Категория | Приоритет | Что делать |
|-----------|-----------|------------|
| breaking | 🔴 СРОЧНО | Исправить немедленно! |
| bugs | 🟠 Важно | Проверить и исправить |
| api | 🟡 Обычно | Обновить DTO если нужно |

## После исправления — отправь подтверждение:
```bash
curl -X POST http://46.17.99.76:8000/api/v1/shared/context \
  -H "Content-Type: application/json" \
  -d '{
    "category": "done",
    "title": "✅ Исправлено: [что]",
    "content": "Исправил [описание]",
    "source": "frontend",
    "priority": 5
  }'
```
