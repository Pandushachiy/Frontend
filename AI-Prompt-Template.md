# AI Prompt Template для Cursor AI

> **НАЗНАЧЕНИЕ**: Шаблон для эффективной работы с Cursor AI при разработке UI

---

## 🎯 Как правильно давать задачи AI

### ❌ ПЛОХО (размытая задача)

```
Сделай красивую карточку для настроек
```

### ✅ ХОРОШО (конкретная задача со ссылками)

```
Создай компонент SettingsCard согласно Design System Guide.

Требования:
- Используй цвета из MaterialTheme.colorScheme
- Отступы из Spacing (cardPadding, medium)
- Скругление CornerRadius.medium
- Анимация появления: fadeIn + slideInVertically (NORMAL duration)
- Компонент должен принимать: title, subtitle, icon, onClick

Ориентируйся на пример из Component-Catalog.md раздел "SettingsCard"
```

---

## 📝 Универсальный шаблон промпта

Используй этот шаблон для КАЖДОЙ UI задачи:

```markdown
## Задача
[Что именно нужно сделать - один конкретный компонент или экран]

## Референсы
- Design System Guide - [конкретный раздел, например "Typography System"]
- Animation Guidelines - [конкретный раздел, например "Fade animations"]
- Component Catalog - [похожий компонент для референса]

## Требования
- Цвета: [какие именно из MaterialTheme.colorScheme]
- Отступы: [какие именно из Spacing]
- Скругления: [из CornerRadius]
- Анимации: [какие именно и когда]
- Типографика: [какие стили из Typography]

## Дополнительно
[Любые особые условия или edge cases]

## Чеклист
Перед завершением проверь:
- [ ] Все цвета из colorScheme
- [ ] Все отступы из Spacing
- [ ] Анимация реализована
- [ ] Компонент переиспользуемый
- [ ] Нет магических чисел
```

---

## 🔥 Примеры конкретных промптов

### Пример 1: Создание нового экрана

```markdown
## Задача
Создать экран ProfileScreen с личной информацией пользователя

## Референсы
- Design System Guide - разделы: Color Palette, Spacing System, Typography
- Animation Guidelines - раздел: "Появление карточки при скролле"
- Component Catalog - компоненты: AppCard, AppTopBar, AppAvatar

## Требования
Структура экрана:
1. AppTopBar с заголовком "Профиль" и кнопкой назад
2. AppAvatar размером 80dp по центру
3. Имя пользователя - typography.headlineLarge
4. Email - typography.bodyMedium, цвет onSurfaceVariant
5. 3 карточки с информацией (AppCard):
   - "Мои данные" с иконкой Person
   - "Безопасность" с иконкой Lock
   - "Настройки" с иконкой Settings

Анимации:
- Аватар: fadeIn + scaleIn (0.8f -> 1f) за NORMAL duration
- Карточки: staggered появление (delay 50ms между карточками)
- Каждая карточка: fadeIn + slideInVertically

Цвета и отступы:
- background: colorScheme.background
- screenPadding: Spacing.screenPadding
- gap между карточками: Spacing.medium
- Внутри карточки: Spacing.cardPadding

## Чеклист
- [ ] Все компоненты из Component Catalog
- [ ] Все значения из Design System
- [ ] Анимации работают
- [ ] Экран адаптивен к темной теме
```

---

### Пример 2: Исправление существующего компонента

```markdown
## Задача
Исправить компонент ChatMessageCard - сейчас он использует произвольные отступы и цвета

## Проблемы
1. padding(10.dp) - должно быть Spacing.small или medium
2. Color(0xFF4ECDC4) - должен быть colorScheme.primary
3. fontSize = 15.sp - должен быть typography.bodyMedium
4. Нет анимации появления

## Референсы
- Design System Guide - Color Palette, Spacing System, Typography System
- Animation Guidelines - раздел "Чат-сообщения (появление снизу)"
- Component Catalog - примеры других Card компонентов

## Требования
Заменить:
- padding(10.dp) → padding(Spacing.medium)
- Color(0xFF4ECDC4) → MaterialTheme.colorScheme.primary
- fontSize = 15.sp → style = MaterialTheme.typography.bodyMedium
- Добавить анимацию: slideInVertically + fadeIn за NORMAL duration

## Чеклист
- [ ] Все магические числа заменены на Spacing
- [ ] Все цвета из colorScheme
- [ ] Typography стили применены
- [ ] Анимация добавлена
- [ ] Компонент работает как раньше (функционально)
```

---

### Пример 3: Добавление анимации

```markdown
## Задача
Добавить анимацию нажатия (press effect) к кнопкам на экране SettingsScreen

## Референсы
- Animation Guidelines - раздел "Scale (Масштабирование)" и "Кнопки (Press effect)"
- Component Catalog - AppButton с готовой реализацией

## Требования
Для каждой кнопки на экране:
1. При нажатии scale должен стать 0.95f
2. При отпускании вернуться к 1f
3. Использовать spring animation:
   - dampingRatio = Spring.DampingRatioMediumBouncy
   - stiffness = Spring.StiffnessMedium
4. Применить через Modifier.scale() и pointerInput

## Пример кода
Используй реализацию из Component-Catalog.md, секция "AppButton"

## Чеклист
- [ ] Spring animation с правильными параметрами
- [ ] Работает на всех кнопках экрана
- [ ] Не мешает существующим onClick handlers
- [ ] Анимация плавная и естественная
```

---

## 🎨 Промпты для типичных задач

### Задача: Создать новую карточку

```markdown
Создай компонент [НазваниеCard] по аналогии с AppCard из Component Catalog.

Особенности:
- [Описание что внутри: иконка, текст, кнопка и т.д.]
- [Когда использовать]

Следуй Design System:
- Цвета: surface/onSurface
- Padding: cardPadding
- CornerRadius: medium
- Elevation: level1
- Анимация: fadeIn + slideInVertically (NORMAL)

Сделай компонент переиспользуемым с параметрами для кастомизации.
```

---

### Задача: Исправить отступы на экране

```markdown
На экране [НазваниеScreen] отступы не соответствуют Design System.

Проверь и исправь:
- Отступ от краёв экрана → Spacing.screenPadding (16dp)
- Между карточками → Spacing.medium (16dp)
- Внутри карточек → Spacing.cardPadding (16dp)
- Между элементами внутри карточки → Spacing.small (8dp)
- Между секциями → Spacing.large (24dp)

Замени все произвольные значения dp на константы из Spacing.

Визуальный результат должен остаться похожим, но используй правильные значения.
```

---

### Задача: Стандартизировать цвета

```markdown
На экране [НазваниеScreen] используются хардкод цвета вместо MaterialTheme.

Замени все Color(...) на MaterialTheme.colorScheme:
- Фон карточки → surface
- Текст заголовка → onSurface
- Текст описания → onSurfaceVariant
- Акцентный цвет → primary
- Текст на акценте → onPrimary
- Границы → outline
- Иконки неактивные → onSurfaceVariant

Референс: Design System Guide раздел "Правила использования цветов"
```

---

### Задача: Добавить навигационные переходы

```markdown
В NavHost отсутствуют анимации переходов между экранами.

Добавь NavAnimations согласно Animation Guidelines раздел "Навигационные переходы":

enterTransition: slideInHorizontally (справа) + fadeIn
exitTransition: slideOutHorizontally (влево немного) + fadeOut
popEnterTransition: slideInHorizontally (слева) + fadeIn
popExitTransition: slideOutHorizontally (вправо) + fadeOut

Длительность: MODERATE (500ms)
Easing: Standard

Примени ко всем переходам в NavHost.
```

---

### Задача: Создать Bottom Sheet

```markdown
Создай Bottom Sheet компонент для экрана [НазваниеScreen].

Используй готовый AppBottomSheet из Component Catalog.

Контент sheet:
- [Заголовок]
- [Описание элементов]
- [Кнопки]

Требования:
- Shape: topStart и topEnd с CornerRadius.extraLarge
- backgroundColor: surface
- Появление: slideInVertically снизу за MODERATE duration
- Drag handle: стандартный BottomSheetDefaults.DragHandle

Пример использования из Component-Catalog.md
```

---

## 🚨 Частые ошибки AI и как их избежать

### Проблема 1: AI использует произвольные цвета

**Неправильный промпт:**
```
Сделай кнопку с синим цветом
```

**Правильный промпт:**
```
Сделай кнопку используя MaterialTheme.colorScheme.primary для фона 
и colorScheme.onPrimary для текста. 
НЕ используй Color(0x...) - только colorScheme!
```

---

### Проблема 2: AI не добавляет анимации

**Неправильный промпт:**
```
Создай карточку с анимацией
```

**Правильный промпт:**
```
Создай карточку с анимацией появления:
- fadeIn(tween(AnimationDuration.NORMAL))
- slideInVertically(initialOffsetY = { it / 3 }, tween(NORMAL, AppEasing.Decelerate))
Используй AnimatedVisibility с LaunchedEffect для задержки 50ms.
Пример в Animation Guidelines раздел "Появление карточки".
```

---

### Проблема 3: AI создаёт нестандартные компоненты

**Неправильный промпт:**
```
Создай кнопку для этого экрана
```

**Правильный промпт:**
```
Используй готовый компонент AppButton из Component Catalog.
НЕ создавай новую кнопку, используй существующий компонент.
Передай в него параметры: text, onClick, icon.
```

---

## 🎓 Обучение AI контексту проекта

### В начале КАЖДОЙ новой сессии Cursor дай AI этот промпт:

```markdown
# Контекст проекта

Это Android приложение на Kotlin + Jetpack Compose.

У нас есть Design System с готовыми значениями:
- Цвета: MaterialTheme.colorScheme (НЕ используй Color(0x...))
- Отступы: Spacing.small/medium/large (НЕ используй произвольные dp)
- Скругления: CornerRadius.medium/large (НЕ используй произвольные dp)
- Типографика: MaterialTheme.typography (НЕ используй fontSize/fontWeight напрямую)
- Анимации: AnimationDuration + AppEasing (НЕ используй произвольные ms)

Файлы для референса:
- DesignSystem-Guide.md - все константы и правила
- Animation-Guidelines.md - все анимации
- Component-Catalog.md - готовые компоненты

КРИТИЧЕСКИ ВАЖНО:
- Всегда проверяй эти файлы перед генерацией кода
- НЕ изобретай новые значения, используй существующие
- НЕ создавай новые компоненты если есть готовые
- Все UI должны быть консистентными

Ты понял контекст?
```

**Дождись подтверждения от AI, затем давай задачу.**

---

## 🔄 Промпт для рефакторинга существующего кода

```markdown
## Задача: Рефакторинг [НазваниеScreen]

Сейчас экран не соответствует Design System.

Проверь и исправь:

1. **Цвета**
   - Все Color(0x...) → MaterialTheme.colorScheme.*
   - Ориентируйся на таблицу в DesignSystem-Guide.md "Правила использования цветов"

2. **Отступы**
   - Все .dp значения → Spacing.*
   - Проверь раздел "Spacing System" в Design System

3. **Типографика**
   - Все fontSize/fontWeight → MaterialTheme.typography.*
   - Проверь раздел "Typography System"

4. **Скругления**
   - Все RoundedCornerShape(...dp) → RoundedCornerShape(CornerRadius.*)

5. **Анимации**
   - Добавь где нужно согласно Animation Guidelines
   - Все длительности → AnimationDuration.*

6. **Компоненты**
   - Замени кастомные UI на готовые из Component Catalog

Сохрани функциональность, измени только стилизацию.

Пройдись по чеклисту из DesignSystem-Guide.md раздел "Чеклист перед коммитом".
```

---

## 📊 Как проверять результат работы AI

### Чеклист после каждого ответа AI:

```markdown
## Проверь сам перед использованием кода:

- [ ] Нет Color(0x...) - только colorScheme
- [ ] Нет произвольных .dp - только Spacing/CornerRadius
- [ ] Нет fontSize/fontWeight - только typography
- [ ] Анимации используют AnimationDuration
- [ ] Компоненты из Component Catalog где возможно
- [ ] Есть label в animated*AsState
- [ ] Код форматирован и читаем

Если что-то не так - попроси AI исправить со ссылкой на конкретный раздел Guide.
```

---

## 🎯 Быстрые команды для AI

Сохрани эти короткие промпты для частых задач:

### "Fix spacing"
```
Исправь все отступы согласно Spacing из Design System Guide.
Замени все произвольные .dp на константы.
```

### "Fix colors"
```
Исправь все цвета согласно MaterialTheme.colorScheme из Design System Guide.
Удали все Color(0x...).
```

### "Add animation"
```
Добавь анимацию появления согласно Animation Guidelines.
Используй fadeIn + slideInVertically, NORMAL duration.
```

### "Use component"
```
Замени этот UI на компонент [название] из Component Catalog.
НЕ создавай новый, используй существующий.
```

### "Follow design system"
```
Перепиши этот компонент полностью согласно Design System Guide.
Проверь все: цвета, отступы, типографику, анимации.
```

---

## 🔁 Работа с контекстом AI (важно!)

### Проблема: AI забывает Design System через несколько сообщений

**Решение: Каждые 5-7 сообщений напоминай:**

```markdown
Напоминаю: используй только значения из Design System:
- Цвета: MaterialTheme.colorScheme
- Отступы: Spacing
- Типографика: MaterialTheme.typography
- Анимации: AnimationDuration + AppEasing

Проверь последний ответ на соответствие.
```

---

### Проблема: AI создаёт слишком сложный код

**Решение:**

```markdown
Упрости код:
- Используй готовые компоненты из Component Catalog
- Не создавай дополнительные вложенности
- Один компонент = одна ответственность
- Используй remember только когда действительно нужно
```

---

## 💡 Лучшие практики промптов

1. **Будь конкретным**
   - ❌ "Сделай красиво"
   - ✅ "Используй colorScheme.primary с elevation level2"

2. **Ссылайся на документацию**
   - ❌ "Добавь анимацию"
   - ✅ "Добавь анимацию как в Animation Guidelines раздел X"

3. **Проверяй результат**
   - После каждого ответа проверь по чеклисту
   - Если что-то не так - укажи на конкретную проблему

4. **Используй примеры**
   - ❌ "Сделай как в других карточках"
   - ✅ "Сделай как SettingsCard из Component Catalog"

5. **Напоминай о контексте**
   - Каждые несколько сообщений напоминай про Design System
   - Особенно после длинных дискуссий

---

## 🎬 Итоговый универсальный промпт

**Используй этот шаблон для ЛЮБОЙ UI задачи:**

```markdown
## [ЗАДАЧА ОДНИМ ПРЕДЛОЖЕНИЕМ]

Референсы:
- Design System Guide: [конкретные разделы]
- Animation Guidelines: [конкретные разделы]
- Component Catalog: [похожие компоненты]

Требования:
- Цвета: [какие из colorScheme]
- Отступы: [какие из Spacing]
- Типографика: [какие из typography]
- Анимации: [какие именно]
- Компоненты: [какие использовать]

ВАЖНО:
- НЕ используй произвольные значения
- ИСПОЛЬЗУЙ только константы из Design System
- ПРОВЕРЬ по чеклисту перед ответом

Понятна задача?
```

---

**ГЛАВНОЕ**: Cursor AI - мощный инструмент, но ему нужен чёткий контекст и конкретные инструкции. Используй эти шаблоны промптов и регулярно напоминай про Design System.
