# 🏛️ Decision Log — Архитектурные решения

---

## [2026-01-26] SSE Streaming через OkHttp

**Решение:** Использовать `okhttp3-sse` вместо Retrofit для стриминга

**Причина:** Retrofit не поддерживает SSE нативно

**Реализация:**
```kotlin
val streamClient = okHttpClient.newBuilder()
    .readTimeout(0, TimeUnit.SECONDS)
    .build()
```

---

## [2026-01-26] Room как кэш, сервер — источник правды

**Решение:** При `getConversations()`:
1. Получить данные с сервера
2. Удалить локальные записи, которых нет на сервере
3. Upsert серверные данные в Room

**Причина:** Избежать дубликатов и рассинхрона

---

## [2026-01-26] Защита от перезаписи сообщений при стриминге

**Решение:** В `observeCurrentConversationMessages`:
```kotlin
if (messagesList.size >= _messages.value.size) {
    _messages.value = messagesList
}
```

**Причина:** Room Flow мог перезаписывать сообщения старыми данными во время стриминга

---

## [2026-01-26] Portrait-only режим

**Решение:** `android:screenOrientation="portrait"` в AndroidManifest

**Причина:** UI оптимизирован только под портретную ориентацию
