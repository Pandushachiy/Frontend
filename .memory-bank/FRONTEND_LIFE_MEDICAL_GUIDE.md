{"filename":"FRONTEND_LIFE_MEDICAL_GUIDE.md","content":"# 🌟 Frontend Guide: Life Context & Medical Assistant\n\n## Обзор новых фич\n\n### 1. Life Context — Контекст жизни\nAI теперь глубже понимает пользователя через:\n- Анкету профиля (базовая инфо, здоровье, цели, окружение)\n- Важные даты (ДР близких, годовщины)\n- Важные люди (семья, друзья)\n- Паттерны жизни (активность, настроение)\n\n### 2. Medical Assistant — Медицинский помощник\nAI может помочь с:\n- Анализом симптомов (информационно!)\n- Проверкой взаимодействий лекарств\n- Объяснением результатов анализов\n- Поиском медицинской информации\n\n⚠️ **ВАЖНО**: Не заменяет врача! Только информационная поддержка.\n\n---\n\n## 📋 API: Life Context\n\n### Получить анкету\n```typescript\n// GET /api/v1/life-context/questionnaire\n// Можно указать section: basic, health, lifestyle, social, goals, mental, preferences, important_dates\n\ninterface Question {\n  key: string;\n  question: string;\n  type: 'text' | 'number' | 'choice' | 'list' | 'textarea' | 'slider' | 'boolean' | 'date' | 'date_list';\n  options?: string[];  // для choice\n  min?: number;        // для slider\n  max?: number;        // для slider\n}\n\ninterface QuestionnaireSection {\n  title: string;\n  questions: Question[];\n}\n\n// Response\n{\n  \"basic\": { title: \"👤 Базовая информация\", questions: [...] },\n  \"health\": { title: \"🏥 Здоровье\", questions: [...] },\n  ...\n}\n```\n\n### Сохранить ответы\n```typescript\n// POST /api/v1/life-context/questionnaire\ninterface QuestionnaireRequest {\n  answers: Record<string, any>;\n}\n\n// Example\n{\n  \"answers\": {\n    \"preferred_name\": \"Дима\",\n    \"age\": 28,\n    \"location\": \"Москва\",\n    \"health_conditions\": [\"мигрень\"],\n    \"short_term_goals\": [\"выучить TypeScript\", \"сбросить 5 кг\"]\n  }\n}\n\n// Response\n{ \"success\": true, \"message\": \"Профиль обновлён\", \"profile_version\": 3 }\n```\n\n### Добавить важную дату\n```typescript\n// POST /api/v1/life-context/important-dates\ninterface ImportantDateCreate {\n  date: string;      // \"2026-03-15\"\n  title: string;     // \"День рождения мамы\"\n  event_type?: string; // \"birthday\" | \"anniversary\" | \"custom\"\n  recurring?: boolean; // true = повторяется ежегодно\n}\n\n// Response\n{ \"success\": true, \"message\": \"Дата 'День рождения мамы' добавлена\" }\n```\n\n### Добавить важного человека\n```typescript\n// POST /api/v1/life-context/important-people\ninterface ImportantPersonCreate {\n  name: string;     // \"Мама\"\n  relation: string; // \"мама\"\n  details?: string; // \"Любит цветы, звонить по субботам\"\n}\n```\n\n### Паттерны жизни\n```typescript\n// GET /api/v1/life-context/patterns\ninterface LifePatternsResponse {\n  mood_trend: 'improving' | 'stable' | 'declining' | null;\n  avg_mood: number | null;     // 1-10\n  avg_stress: number | null;   // 1-10\n  active_times: Array<{ hour: number; messages: number }>;\n  recommendations: string[];\n}\n```\n\n---\n\n## 🏥 API: Medical Assistant\n\n### Проверка симптомов\n```typescript\n// POST /api/v1/medical/symptoms\ninterface SymptomCheckRequest {\n  symptoms: string;       // \"Болит голова уже 3 дня, тошнит\"\n  duration?: string;      // \"3 дня\"\n}\n\ninterface SymptomCheckResponse {\n  symptoms: string[];\n  severity: 'low' | 'medium' | 'high' | 'urgent';\n  recommendations: string[];\n  when_to_see_doctor: string;\n  specialist_type: string | null;\n  disclaimer: string;\n}\n```\n\n### Проверка взаимодействий лекарств\n```typescript\n// POST /api/v1/medical/drug-interactions\ninterface DrugInteractionRequest {\n  drugs: string[];                    // [\"ибупрофен\", \"аспирин\"]\n  include_current_medications?: boolean; // учитывать лекарства из профиля\n}\n\ninterface DrugInteractionResponse {\n  drugs_checked: string[];\n  warnings: string[];           // [\"⚠️ ОПАСНО: ...\"]\n  detailed_analysis?: string;   // от Perplexity\n  disclaimer: string;\n}\n```\n\n### Объяснение анализов\n```typescript\n// POST /api/v1/medical/lab-results\ninterface LabResultsRequest {\n  results: Record<string, number>;  // { \"гемоглобин\": 145, \"глюкоза\": 5.2 }\n}\n\ninterface LabResultsResponse {\n  analyses: Array<{\n    name: string;\n    value: number;\n    unit: string;\n    status: 'normal' | 'low' | 'high' | 'critical_low' | 'critical_high';\n    reference_low?: number;\n    reference_high?: number;\n  }>;\n  critical_values: string[];   // Критические отклонения\n  concerns: string[];          // Отклонения от нормы\n  explanation: string;         // Подробное объяснение\n  recommendation: string;\n  disclaimer: string;\n}\n```\n\n### Поиск медицинской информации\n```typescript\n// POST /api/v1/medical/search\ninterface MedicalSearchRequest {\n  query: string;  // \"Что такое мигрень и как её лечить?\"\n}\n\ninterface MedicalSearchResponse {\n  query: string;\n  information: string;\n  citations: string[];\n  disclaimer: string;\n}\n```\n\n### Рекомендации по здоровью\n```typescript\n// GET /api/v1/medical/recommendations?focus_area=сон\n// focus_area: питание, сон, стресс, активность, профилактика\n\ninterface HealthRecommendationsResponse {\n  focus_area: string;\n  recommendations: string;\n  personalized: boolean;\n  disclaimer: string;\n}\n```\n\n### Экстренная информация\n```typescript\n// GET /api/v1/medical/emergency-info\ninterface EmergencyInfo {\n  emergency_numbers: {\n    ambulance: \"103\";\n    universal: \"112\";\n    poison_control: \"8-495-628-16-87\";\n  };\n  when_to_call_emergency: string[];\n  fast_stroke_check: {\n    F: string;  // Face\n    A: string;  // Arms\n    S: string;  // Speech\n    T: string;  // Time\n  };\n  disclaimer: string;\n}\n```\n\n---\n\n## 🎨 UI Рекомендации\n\n### Анкета профиля\n\n```tsx\n// Компонент для пошагового заполнения\n\nconst ProfileQuestionnaire = () => {\n  const sections = ['basic', 'health', 'lifestyle', 'social', 'goals', 'mental'];\n  const [currentSection, setCurrentSection] = useState(0);\n  \n  return (\n    <div className=\"questionnaire\">\n      <ProgressBar current={currentSection} total={sections.length} />\n      \n      <SectionTitle>{questionnaire[sections[currentSection]].title}</SectionTitle>\n      \n      {questionnaire[sections[currentSection]].questions.map(q => (\n        <QuestionField key={q.key} question={q} />\n      ))}\n      \n      <NavigationButtons\n        onBack={() => setCurrentSection(s => s - 1)}\n        onNext={() => setCurrentSection(s => s + 1)}\n        onSkip={() => setCurrentSection(s => s + 1)}\n      />\n    </div>\n  );\n};\n```\n\n### Медицинский помощник\n\n```tsx\n// Карточка проверки симптомов\n\nconst SymptomChecker = () => {\n  return (\n    <Card className=\"symptom-checker\">\n      <Header>\n        <Icon name=\"stethoscope\" />\n        <Title>Проверка симптомов</Title>\n      </Header>\n      \n      <Disclaimer type=\"warning\">\n        ⚠️ Это НЕ диагностика! Только информационная поддержка.\n      </Disclaimer>\n      \n      <TextArea\n        placeholder=\"Опишите что беспокоит...\"\n        value={symptoms}\n        onChange={setSymptoms}\n      />\n      \n      <TextField\n        label=\"Как давно? (опционально)\"\n        placeholder=\"Например: 3 дня\"\n        value={duration}\n        onChange={setDuration}\n      />\n      \n      <Button onClick={checkSymptoms}>\n        Проверить\n      </Button>\n      \n      {result && (\n        <ResultCard severity={result.severity}>\n          <SeverityBadge severity={result.severity} />\n          <Recommendations items={result.recommendations} />\n          <DoctorAdvice when={result.when_to_see_doctor} specialist={result.specialist_type} />\n          <Disclaimer>{result.disclaimer}</Disclaimer>\n        </ResultCard>\n      )}\n    </Card>\n  );\n};\n```\n\n### Цветовая схема для severity\n\n```css\n/* Для медицинских результатов */\n.severity-low { \n  background: #E8F5E9; \n  border-color: #4CAF50; \n}\n\n.severity-medium { \n  background: #FFF3E0; \n  border-color: #FF9800; \n}\n\n.severity-high { \n  background: #FFEBEE; \n  border-color: #F44336; \n}\n\n.severity-urgent { \n  background: #B71C1C; \n  color: white;\n  animation: pulse 1s infinite;\n}\n\n/* Для анализов */\n.lab-normal { color: #4CAF50; }\n.lab-low { color: #2196F3; }\n.lab-high { color: #FF9800; }\n.lab-critical { color: #F44336; font-weight: bold; }\n```\n\n---\n\n## 📱 Экраны для реализации\n\n### 1. Профиль с анкетой\n```\n┌─────────────────────────────┐\n│ 👤 Мой профиль              │\n├─────────────────────────────┤\n│ Имя: Дима                   │\n│ Возраст: 28                 │\n│ Город: Москва               │\n│                             │\n│ 🏥 Здоровье                 │\n│ • Мигрень                   │\n│ • Аллергия: пыльца          │\n│                             │\n│ 🎯 Цели                     │\n│ • Выучить TypeScript        │\n│ • Сбросить 5 кг             │\n│                             │\n│ [📝 Редактировать профиль]  │\n│ [📅 Важные даты]            │\n│ [👥 Близкие люди]           │\n└─────────────────────────────┘\n```\n\n### 2. Медицинский помощник\n```\n┌─────────────────────────────┐\n│ 🏥 Медицинский помощник     │\n├─────────────────────────────┤\n│ ⚠️ Не заменяет врача!       │\n├─────────────────────────────┤\n│                             │\n│ [🔍 Проверить симптомы]     │\n│                             │\n│ [💊 Проверить лекарства]    │\n│                             │\n│ [🔬 Объяснить анализы]      │\n│                             │\n│ [💪 Рекомендации]           │\n│                             │\n│ [🚨 Экстренная помощь]      │\n│                             │\n└─────────────────────────────┘\n```\n\n### 3. Результат проверки симптомов\n```\n┌─────────────────────────────┐\n│ Результат                   │\n├─────────────────────────────┤\n│ Серьёзность: 🟡 Средняя     │\n├─────────────────────────────┤\n│ Возможные причины:          │\n│ • Напряжённая головная боль │\n│ • Мигрень                   │\n│ • Усталость глаз            │\n├─────────────────────────────┤\n│ Рекомендации:               │\n│ • Отдохните в тихом месте   │\n│ • Пейте больше воды         │\n│ • Проветрите помещение      │\n├─────────────────────────────┤\n│ К врачу: В ближайшие 2-3 дн │\n│ Специалист: Невролог        │\n├─────────────────────────────┤\n│ ⚠️ Это информация, не       │\n│ диагноз. Обратитесь к врачу!│\n└─────────────────────────────┘\n```\n\n---\n\n## 🔗 Интеграция в чат\n\nAI автоматически использует:\n- **Life Context** — понимает контекст жизни пользователя\n- **Medical Context** — учитывает аллергии, лекарства при медицинских вопросах\n- **Important Dates** — может напомнить о приближающихся событиях\n\n### Примеры фраз в чате:\n- \"Болит голова уже 3 дня\" → AI проанализирует и порекомендует специалиста\n- \"Можно ли пить нурофен с аспирином?\" → AI проверит взаимодействие\n- \"Что значит глюкоза 6.5?\" → AI объяснит результат\n- \"Какие у меня цели?\" → AI напомнит цели из профиля\n\n---\n\n## ⚡ Готовые компоненты\n\n### SeverityBadge\n```tsx\nconst SeverityBadge = ({ severity }: { severity: string }) => {\n  const config = {\n    low: { color: 'green', icon: '✅', label: 'Лёгкая' },\n    medium: { color: 'orange', icon: '⚡', label: 'Средняя' },\n    high: { color: 'red', icon: '⚠️', label: 'Серьёзная' },\n    urgent: { color: 'darkred', icon: '🚨', label: 'СРОЧНО' }\n  };\n  \n  const c = config[severity] || config.low;\n  \n  return (\n    <Badge color={c.color}>\n      {c.icon} {c.label}\n    </Badge>\n  );\n};\n```\n\n### LabResultIndicator\n```tsx\nconst LabResultIndicator = ({ value, low, high }: Props) => {\n  const percent = ((value - low) / (high - low)) * 100;\n  const clamped = Math.max(0, Math.min(100, percent));\n  \n  return (\n    <div className=\"lab-indicator\">\n      <div className=\"range-bar\">\n        <div className=\"normal-zone\" />\n        <div className=\"marker\" style={{ left: `${clamped}%` }} />\n      </div>\n      <div className=\"labels\">\n        <span>{low}</span>\n        <span>Норма</span>\n        <span>{high}</span>\n      </div>\n    </div>\n  );\n};\n```\n\n---\n\n## 🚀 Приоритет реализации\n\n1. **Высокий**: Медицинский помощник (проверка симптомов, лекарства)\n2. **Высокий**: Анкета профиля (пошаговое заполнение)\n3. **Средний**: Важные даты и люди\n4. **Низкий**: Паттерны жизни (аналитика)\n\n---\n\n*Документ создан: 30.01.2026*\n*Для фронтенд-команды Pand-AI*\n","size":11765}
